{
  "compilerOptions": {
    "outDir": "./dist/",
    "noImplicitAny": true,
    "module": "ESNext",
    "target": "es5",
    "jsx": "react-jsx", // ! 
    "allowJs": true,
    "baseUrl": ".",
    "paths": {
      "@/*": [
        "./src/*"
      ]
    },
    "moduleResolution": "node",
    "allowSyntheticDefaultImports": true, // для библиотек не имеющих дефолтного импорта. (вместо import * as React - import React) 
    "esModuleInterop": true // позволяет работать с пакетами, которые используют commonJS, как с обычными пакетами (import)
  },
  "ts-node": {
    "compilerOptions": {
      "module": "CommonJS"
    }
  }
}